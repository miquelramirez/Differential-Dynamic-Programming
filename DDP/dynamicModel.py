# Dynamic Model

import numpy as np
from random import uniform

class DynamicModel:
    def __init__(self):
        self.stateNumber = 4
        self.commandNumber = 1

    def computeNextState(self, dt, X, U):
        return nextX

    def computeAIModelDeriv(self, dt, X, U):
        return fx, fu, fxx, fuu, fux, fxu
